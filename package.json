{
  "name": "pagest",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Static site, no build needed'",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.26.0"
  }
}
