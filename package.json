{
  "name": "pagest",
  "version": "1.0.0",
  "scripts": {
    "build": "vite build",       // or your build command
    "deploy": "wrangler publish"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "vite": "^4.0.0"             // optional, if using Vite
  }
}
