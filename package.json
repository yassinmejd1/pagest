{
  "name": "pagest",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Run your build here, e.g. vite build or custom command'",
    "deploy": "wrangler publish"
  },
  "devDependencies": {
    "wrangler": "^3.114.11"
  }
}
